<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TaskNest.MainPage"
             x:Name="ThisPage"
             Title="TaskNest">

    <VerticalStackLayout Padding="20" Spacing="12">
        <HorizontalStackLayout Spacing="8">
            <Entry Placeholder="Add a task" Text="{Binding NewTaskText}" HorizontalOptions="FillAndExpand" />
            <Button Text="Add" Command="{Binding AddTaskCommand}" />
        </HorizontalStackLayout>

        <CollectionView ItemsSource="{Binding Tasks}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout Padding="8" Spacing="12" VerticalOptions="Center">
                        <CheckBox IsChecked="{Binding IsCompleted}" VerticalOptions="Center" />
                        <Label Text="{Binding Title}" VerticalOptions="Center" HorizontalOptions="StartAndExpand" />
                        <Button Text="Delete" Command="{Binding BindingContext.RemoveTaskCommand, Source={x:Reference ThisPage}}" CommandParameter="{Binding .}" />
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>

</ContentPage>
